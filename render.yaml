services:
  - type: web
    name: purepoint
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && pnpm run db:migrate && pnpm run build
    startCommand: node dist/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
