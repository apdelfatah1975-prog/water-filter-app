import { useEffect, useMemo, useState } from "react";
import { CalendarDays, Camera, CheckCircle2, CircleAlert, MapPin, Plus, UserRound, Wrench, X } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { formatAppMoney } from "@/lib/appSettings";
import { CustomerContactActions } from "@/components/CustomerContactActions";
import { extractArray } from "@/lib/dataNormalization";

export function splitWorkOrderPhotos(proofs: unknown) {
  const normalized = extractArray<{ kind?: string; photoSlot?: string; url: string; id?: number }>(proofs);
  const photos = normalized.filter(proof => proof.kind === "photo");
  return {
    all: photos,
    before: photos.filter(proof => proof.photoSlot === "before"),
    after: photos.filter(proof => proof.photoSlot === "after"),
  };
}
const serviceOptions = [
  { value: "installation", label: "تركيب فلتر" },
  { value: "maintenance", label: "صيانة" },
  { value: "cartridge_change", label: "تغيير شمعات" },
  { value: "follow_up", label: "متابعة" },
  { value: "other", label: "أخرى" },
] as const;

export default function WorkOrders() {
  const today = new Date().toISOString().slice(0, 10);
  const [customerId, setCustomerId] = useState("");
  const [technicianId, setTechnicianId] = useState("");
  const [visitType, setVisitType] = useState<(typeof serviceOptions)[number]["value"]>("maintenance");
  const [date, setDate] = useState(today);
  const [time, setTime] = useState("09:00");
  const [notes, setNotes] = useState("");
  const [reportFrom, setReportFrom] = useState(today);
  const [reportTo, setReportTo] = useState(today);
  const centralQueryOptions = {
    staleTime: 5_000,
    refetchInterval: 8_000,
    refetchOnReconnect: true,
    refetchOnWindowFocus: false,
    networkMode: "online" as const,
  };
  const customersQuery = trpc.filters.customers.list.useQuery({ followUpStatus: "all", sortBy: "created_desc" }, centralQueryOptions);
  const techniciansQuery = trpc.filters.technicians.list.useQuery(undefined, { ...centralQueryOptions, staleTime: 30_000 });
  const ordersQuery = trpc.filters.workOrders.list.useQuery(undefined, { ...centralQueryOptions, retry: false });
  const notificationSettingsQuery = trpc.filters.notifications.settings.useQuery(undefined, { ...centralQueryOptions, staleTime: 60_000 });
  const dailyClosingQuery = trpc.filters.dailyCashClosing.useQuery({ date: today }, { ...centralQueryOptions, staleTime: 30_000, retry: false });
  const visibleCustomers = extractArray<NonNullable<typeof customersQuery.data>[number]>(customersQuery.data);
  const visibleTechnicians = extractArray<NonNullable<typeof techniciansQuery.data>[number]>(techniciansQuery.data);
  const visibleOrders = extractArray<NonNullable<typeof ordersQuery.data>[number]>(ordersQuery.data);
  const [selectedOrderId, setSelectedOrderId] = useState<number | null>(null);
  const selectedOrder = visibleOrders.find(order => order.id === selectedOrderId) ?? null;
  const [zoomedPhoto, setZoomedPhoto] = useState<string | null>(null);
  const selectedProofs = selectedOrder ? extractArray(selectedOrder.proofs) : [];
  const photoGroups = splitWorkOrderPhotos(selectedProofs);
  const selectedPhotos = photoGroups.all;
  const beforePhotos = photoGroups.before;
  const afterPhotos = photoGroups.after;
  const utils = trpc.useUtils();
  const createOrder = trpc.filters.workOrders.create.useMutation({
    onSuccess: () => {
      toast.success("تم إرسال أمر العمل إلى صفحة الفني");
      utils.filters.workOrders.list.invalidate();
      setCustomerId("");
      setTechnicianId("");
      setNotes("");
    },
    onError: error => toast.error(error.message || "تعذر إنشاء أمر العمل"),
  });
  const selectedCustomer = useMemo(() => visibleCustomers.find(customer => String(customer.id) === customerId), [visibleCustomers, customerId]);
  const completedOrders = useMemo(() => visibleOrders.filter(order => order.status === "completed" && new Date(order.visitDate).toISOString().slice(0, 10) >= reportFrom && new Date(order.visitDate).toISOString().slice(0, 10) <= reportTo), [visibleOrders, reportFrom, reportTo]);
  const completedTotal = completedOrders.reduce((sum, order) => sum + Number(order.collectedAmount || 0), 0);

  const submit = () => {
    if (!customerId || !technicianId || !date) {
      toast.error("اختر العميل والفني والموعد أولًا");
      return;
    }
    createOrder.mutate({ customerId: Number(customerId), assignedTechnicianId: Number(technicianId), visitType, visitDate: new Date(`${date}T${time}:00`), notes: notes.trim() || null });
  };

  return <main dir="rtl" className="space-y-5 pb-8">
    <section className="rounded-[2rem] bg-[linear-gradient(135deg,#075e59,#0f766e)] p-5 text-white shadow-lg">
      <p className="text-sm font-bold text-teal-100">التشغيل الميداني</p>
      <h1 className="mt-1 text-2xl font-black">أوامر الفنيين</h1>
      <p className="mt-2 text-sm leading-6 text-teal-50">أنشئ أمر تركيب أو صيانة، وسيظهر للفني مع العنوان والموقع والبيانات اللازمة فقط.</p>
    </section>

    <section className="rounded-3xl border border-teal-100 bg-white p-5 shadow-sm">
      <div className="mb-4 flex items-center gap-2"><div className="grid h-10 w-10 place-items-center rounded-2xl bg-teal-50 text-teal-700"><Plus className="h-5 w-5" /></div><div><h2 className="text-lg font-black text-slate-900">إرسال أمر جديد</h2><p className="text-xs font-bold text-slate-500">لن يرى الفني بيانات الخزينة أو تكلفة الشراء</p></div></div>
      <div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">
        <label className="text-sm font-black text-slate-700">العميل<select value={customerId} onChange={event => setCustomerId(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 bg-white px-3 font-bold"><option value="">اختر العميل</option>{visibleCustomers.map(customer => <option key={customer.id} value={customer.id}>{customer.name}{customer.manualCode ? ` — ${customer.manualCode}` : ""}</option>)}</select></label>
        <label className="text-sm font-black text-slate-700">الفني<select value={technicianId} onChange={event => setTechnicianId(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 bg-white px-3 font-bold"><option value="">اختر الفني</option>{visibleTechnicians.map(technician => <option key={technician.id} value={technician.id}>{technician.name}</option>)}</select></label>
        <label className="text-sm font-black text-slate-700">نوع الخدمة<select value={visitType} onChange={event => setVisitType(event.target.value as typeof visitType)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 bg-white px-3 font-bold">{serviceOptions.map(option => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>
        <div className="grid grid-cols-2 gap-2"><label className="text-sm font-black text-slate-700">التاريخ<input type="date" value={date} onChange={event => setDate(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-3 font-bold" /></label><label className="text-sm font-black text-slate-700">الوقت<input type="time" value={time} onChange={event => setTime(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-3 font-bold" /></label></div>
        <label className="text-sm font-black text-slate-700 md:col-span-2">تعليمات للفني<textarea value={notes} onChange={event => setNotes(event.target.value)} placeholder="مثال: فحص تسريب أسفل الحوض" className="mt-2 min-h-24 w-full rounded-xl border border-slate-200 p-3 font-bold" /></label>
      </div>
      {selectedCustomer ? <div className="mt-4 rounded-2xl bg-slate-50 p-3 text-sm font-bold text-slate-600"><div className="flex items-center gap-2"><UserRound className="h-4 w-4 text-teal-700" />{selectedCustomer.name} — {selectedCustomer.phone || "بدون هاتف"}</div><div className="mt-2 flex items-start gap-2"><MapPin className="mt-0.5 h-4 w-4 shrink-0 text-teal-700" />{selectedCustomer.address || "العنوان غير مسجل"}</div><CustomerContactActions customer={selectedCustomer ?? {}} compact labels className="mt-3" /></div> : null}
      <Button type="button" onClick={submit} disabled={createOrder.isPending} className="mt-4 h-12 w-full rounded-xl bg-teal-700 font-black hover:bg-teal-800">{createOrder.isPending ? "جاري الإرسال..." : "إرسال الأمر إلى الفني"}</Button>
    </section>

    <section className="rounded-3xl border border-emerald-100 bg-emerald-50/60 p-5 shadow-sm"><div className="flex items-center gap-2"><div className="grid h-10 w-10 place-items-center rounded-2xl bg-emerald-100 text-emerald-700"><Wrench className="h-5 w-5" /></div><div><h2 className="text-lg font-black text-slate-900">ما تم تنفيذه خلال فترة</h2><p className="text-xs font-bold text-slate-600">تقرير موحد لجميع العملاء والفنيين حسب تاريخ تنفيذ الزيارة.</p></div></div><div className="mt-4 grid gap-3 md:grid-cols-3"><label className="text-sm font-black text-slate-700">من تاريخ<input type="date" value={reportFrom} onChange={event => setReportFrom(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-emerald-200 bg-white px-3 font-bold" /></label><label className="text-sm font-black text-slate-700">إلى تاريخ<input type="date" value={reportTo} onChange={event => setReportTo(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-emerald-200 bg-white px-3 font-bold" /></label><div className="rounded-2xl bg-white p-3"><p className="text-xs font-bold text-slate-500">النتيجة</p><p className="mt-1 text-xl font-black text-emerald-700">{completedOrders.length} زيارة مكتملة</p><p className="text-xs font-bold text-slate-600">إجمالي التحصيل: {formatAppMoney(completedTotal)}</p></div></div>{completedOrders.length ? <div className="mt-4 overflow-x-auto rounded-2xl bg-white"><table className="w-full min-w-[680px] text-right text-sm"><thead className="bg-emerald-100/60 text-xs text-emerald-950"><tr><th className="px-3 py-2">التاريخ</th><th className="px-3 py-2">العميل</th><th className="px-3 py-2">الفني</th><th className="px-3 py-2">ما تم تنفيذه</th><th className="px-3 py-2">المبلغ</th></tr></thead><tbody>{completedOrders.map(order => <tr key={`report-${order.id}`} className="border-t border-emerald-50 align-top"><td className="px-3 py-2 font-bold">{new Date(order.visitDate).toLocaleDateString("ar-EG")}</td><td className="px-3 py-2 font-black">{order.customer?.name || "—"}</td><td className="px-3 py-2 font-bold">{order.technicianName || "—"}</td><td className="max-w-[320px] whitespace-pre-wrap px-3 py-2">{order.visitResult?.trim() || "لم تسجل نتيجة"}</td><td className="px-3 py-2 font-black text-emerald-700">{formatAppMoney(Number(order.collectedAmount || 0))}</td></tr>)}</tbody></table></div> : <div className="mt-4 rounded-2xl bg-white p-4 text-center text-sm font-bold text-slate-500">لا توجد زيارات مكتملة خلال الفترة المحددة.</div>}</section>

    <section className="rounded-3xl border border-sky-100 bg-sky-50/60 p-5 shadow-sm"><div className="flex items-center justify-between gap-3"><div><h2 className="text-lg font-black text-slate-900">تقفيل النقدية اليومي</h2><p className="mt-1 text-xs font-bold text-slate-600">ملخص التحصيل والزيارات المكتملة حسب الفني خلال {today}.</p></div><div className="text-left"><p className="text-xs font-bold text-slate-500">الإجمالي</p><p className="text-lg font-black text-sky-800">{formatAppMoney(Number(dailyClosingQuery.data?.totalCollected || 0))}</p></div></div>{dailyClosingQuery.isLoading ? <div className="mt-4 rounded-2xl bg-white p-4 text-center text-sm font-bold text-slate-500">جاري تجهيز التقفيل...</div> : dailyClosingQuery.data?.technicians?.length ? <div className="mt-4 overflow-x-auto rounded-2xl bg-white"><table className="w-full min-w-[520px] text-right text-sm"><thead className="bg-sky-100/70 text-xs text-sky-950"><tr><th className="px-3 py-2">الفني</th><th className="px-3 py-2">الزيارات المكتملة</th><th className="px-3 py-2">التحصيل</th></tr></thead><tbody>{dailyClosingQuery.data.technicians.map(row => <tr key={row.technicianName} className="border-t border-sky-50"><td className="px-3 py-2 font-black">{row.technicianName}</td><td className="px-3 py-2 font-bold">{row.visitsCompleted}</td><td className="px-3 py-2 font-black text-sky-800">{formatAppMoney(Number(row.collectedAmount || 0))}</td></tr>)}</tbody></table></div> : <div className="mt-4 rounded-2xl bg-white p-4 text-center text-sm font-bold text-slate-500">لا توجد زيارات أو تحصيلات مسجلة اليوم.</div>}</section>

    <section className="space-y-3"><div className="flex items-center gap-2"><Wrench className="h-5 w-5 text-teal-700" /><h2 className="text-lg font-black text-slate-900">الأوامر المرسلة</h2></div><p className="text-xs font-bold text-slate-500">اضغط على أي زيارة لعرض تفاصيلها كاملة.</p><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">{visibleOrders.map(order => { const isNotCompleted = order.executionOutcome === "not_completed"; return <article key={order.id} role="button" tabIndex={0} onClick={() => setSelectedOrderId(order.id)} onKeyDown={event => { if (event.key === "Enter" || event.key === " ") setSelectedOrderId(order.id); }} className="cursor-pointer rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:border-teal-300 hover:shadow-md focus:outline-none focus:ring-2 focus:ring-teal-500"><div className="flex items-start justify-between gap-3"><div><h3 className="font-black text-slate-900">{order.customer?.name || "عميل غير معروف"}</h3><p className="mt-1 text-xs font-bold text-slate-500">{serviceOptions.find(option => option.value === order.visitType)?.label || order.visitType} · {order.technicianName || "بدون فني"}</p></div><span className={`rounded-full px-3 py-1 text-xs font-black ${isNotCompleted ? "bg-amber-100 text-amber-800" : order.status === "completed" ? "bg-emerald-100 text-emerald-800" : "bg-teal-50 text-teal-800"}`}>{isNotCompleted ? "لم يتم التنفيذ" : order.status === "completed" ? "مكتمل" : order.status === "assigned" ? "مسند" : "قيد التنفيذ"}</span></div><div className="mt-3 grid gap-2 text-xs font-bold text-slate-500 sm:grid-cols-2"><span className="flex items-center gap-1"><CalendarDays className="h-4 w-4 text-teal-700" />{new Date(order.visitDate).toLocaleString("ar-EG", { dateStyle: "medium", timeStyle: "short" })}</span><span className="flex items-center gap-1"><MapPin className="h-4 w-4 text-teal-700" />{order.customer?.address || "بدون عنوان"}</span></div><div className="mt-3 flex flex-wrap items-center gap-2 text-xs font-black"><CustomerContactActions customer={order.customer ?? {}} serviceType={order.visitType} companyWhatsAppPhone={notificationSettingsQuery.data?.companyWhatsAppPhone} compact labels hideLocationActions className="shrink-0" /><span className="rounded-lg bg-slate-100 px-2 py-1 text-slate-700">الفني: {order.technicianName || "غير محدد"}</span><span className="rounded-lg bg-slate-100 px-2 py-1 text-slate-700">TDS: {order.tdsIn ?? "—"} ← {order.tdsOut ?? "—"}</span><span className="rounded-lg bg-slate-100 px-2 py-1 text-slate-700">التحصيل: {formatAppMoney(Number(order.collectedAmount || 0))}</span>{extractArray(order.proofs).length ? <span className="inline-flex items-center gap-1 rounded-lg bg-blue-50 px-2 py-1 text-blue-800"><Camera className="h-3.5 w-3.5" />{extractArray(order.proofs).length} مرفق</span> : null}</div>{isNotCompleted ? <div className="mt-3 flex items-start gap-2 rounded-xl bg-amber-50 p-3 text-sm font-bold text-amber-900"><CircleAlert className="mt-0.5 h-4 w-4 shrink-0" /><span>سبب عدم التنفيذ: {order.notCompletedReason || "لم يُذكر السبب"}</span></div> : null}</article>; })}</div></section>

    {selectedOrder ? <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/50 p-3 sm:items-center" role="presentation" onClick={() => setSelectedOrderId(null)}><section role="dialog" aria-modal="true" aria-labelledby="visit-details-title" onClick={event => event.stopPropagation()} className="max-h-[92vh] w-full max-w-3xl overflow-y-auto rounded-3xl bg-white p-5 shadow-2xl sm:p-7" dir="rtl"><div className="flex items-start justify-between gap-3 border-b border-slate-100 pb-4"><div><p className="text-xs font-black text-teal-700">تفاصيل الزيارة</p><h2 id="visit-details-title" className="mt-1 text-2xl font-black leading-tight text-slate-950">{selectedOrder.customer?.name || "عميل غير معروف"}</h2><p className="mt-1 text-sm font-bold text-slate-500">{serviceOptions.find(option => option.value === selectedOrder.visitType)?.label || selectedOrder.visitType}</p></div><button type="button" onClick={() => setSelectedOrderId(null)} aria-label="إغلاق" className="rounded-xl p-2 text-slate-500 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-bold text-slate-500">الحالة</p><p className="mt-1 font-black text-slate-900">{selectedOrder.executionOutcome === "not_completed" ? "لم يتم تنفيذ الزيارة" : selectedOrder.status === "completed" ? "تم التنفيذ" : selectedOrder.status === "assigned" ? "مسند للفني" : "قيد التنفيذ"}</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-bold text-slate-500">تاريخ الزيارة</p><p className="mt-1 font-black text-slate-900">{new Date(selectedOrder.visitDate).toLocaleString("ar-EG", { dateStyle: "medium", timeStyle: "short" })}</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-bold text-slate-500">الفني</p><p className="mt-1 font-black text-slate-900">{selectedOrder.technicianName || "غير محدد"}</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-bold text-slate-500">المبلغ المحصل</p><p className="mt-1 font-black text-teal-700">{formatAppMoney(Number(selectedOrder.collectedAmount || 0))}</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-bold text-slate-500">قياسات TDS</p><p className="mt-1 font-black text-slate-900">قبل: {selectedOrder.tdsIn ?? "—"} / بعد: {selectedOrder.tdsOut ?? "—"}</p></div></div><div className="mt-4 space-y-3"><div className="rounded-2xl border border-slate-100 p-3"><p className="text-xs font-bold text-slate-500">بيانات العميل</p><p className="mt-1 font-black text-slate-900">{selectedOrder.customer?.phone || "بدون هاتف"}</p><p className="mt-1 text-sm font-bold text-slate-600">{selectedOrder.customer?.address || "العنوان غير مسجل"}</p><CustomerContactActions customer={selectedOrder.customer ?? {}} serviceType={selectedOrder.visitType} companyWhatsAppPhone={notificationSettingsQuery.data?.companyWhatsAppPhone} compact labels hideLocationActions className="mt-3" /></div>{selectedOrder.notes ? <div className="rounded-2xl border border-slate-100 p-3"><p className="text-xs font-bold text-slate-500">تعليمات وملاحظات</p><p className="mt-1 whitespace-pre-wrap text-sm font-bold text-slate-800">{selectedOrder.notes}</p></div> : null}{selectedOrder.executionOutcome === "not_completed" ? <div className="rounded-2xl border border-amber-200 bg-amber-50 p-3"><p className="flex items-center gap-2 text-xs font-black text-amber-800"><CircleAlert className="h-4 w-4" />سبب عدم التنفيذ</p><p className="mt-1 whitespace-pre-wrap text-sm font-bold text-amber-950">{selectedOrder.notCompletedReason || "لم يُذكر السبب"}</p></div> : null}{selectedOrder.visitResult ? <div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-3"><p className="flex items-center gap-2 text-xs font-black text-emerald-800"><CheckCircle2 className="h-4 w-4" />نتيجة الزيارة</p><p className="mt-1 whitespace-pre-wrap text-sm font-bold text-emerald-950">{selectedOrder.visitResult}</p></div> : null}<div className="rounded-3xl border border-sky-100 bg-sky-50/50 p-4 sm:p-5"><div className="flex items-center gap-2"><Camera className="h-5 w-5 text-sky-700" /><h3 className="text-lg font-black text-slate-950">معرض صور أمر العمل</h3></div><p className="mt-1 text-sm font-bold text-slate-600">توثيق بصري منفصل قبل الصيانة وبعدها.</p>{selectedPhotos.length ? <div className="mt-4 grid gap-4 md:grid-cols-2"><div className="rounded-2xl bg-white p-3"><p className="text-base font-black text-slate-900">صور قبل الصيانة <span className="text-sm text-slate-500">({beforePhotos.length})</span></p>{beforePhotos.length ? <div className="mt-3 grid grid-cols-2 gap-3">{beforePhotos.map(proof => <button type="button" key={proof.id} onClick={() => setZoomedPhoto(proof.url)} className="group rounded-xl text-right focus:outline-none focus:ring-2 focus:ring-sky-500"><img src={proof.url} alt="صورة قبل الصيانة" className="h-40 w-full rounded-xl object-cover ring-1 ring-slate-200 transition group-hover:ring-sky-400" /><span className="mt-1 block text-xs font-black text-slate-600">اضغط للتكبير</span></button>)}</div> : <p className="mt-3 rounded-xl bg-slate-50 p-3 text-sm font-bold text-slate-500">لا توجد صور قبل الصيانة.</p>}</div><div className="rounded-2xl bg-white p-3"><p className="text-base font-black text-slate-900">صور بعد الصيانة <span className="text-sm text-slate-500">({afterPhotos.length})</span></p>{afterPhotos.length ? <div className="mt-3 grid grid-cols-2 gap-3">{afterPhotos.map(proof => <button type="button" key={proof.id} onClick={() => setZoomedPhoto(proof.url)} className="group rounded-xl text-right focus:outline-none focus:ring-2 focus:ring-emerald-500"><img src={proof.url} alt="صورة بعد الصيانة" className="h-40 w-full rounded-xl object-cover ring-1 ring-slate-200 transition group-hover:ring-emerald-400" /><span className="mt-1 block text-xs font-black text-slate-600">اضغط للتكبير</span></button>)}</div> : <p className="mt-3 rounded-xl bg-slate-50 p-3 text-sm font-bold text-slate-500">لا توجد صور بعد الصيانة.</p>}</div></div> : <p className="mt-4 rounded-2xl bg-white p-4 text-center text-sm font-bold text-slate-500">لا توجد صور مرفقة لهذا الأمر.</p>}{selectedProofs.some(proof => proof.kind !== "photo") ? <div className="mt-4 grid gap-3">{selectedProofs.filter(proof => proof.kind !== "photo").map(proof => <div key={proof.id} className="rounded-2xl bg-white p-3">{proof.kind === "audio" ? <audio controls src={proof.url} className="w-full" /> : <a href={proof.url} target="_blank" rel="noreferrer" className="text-sm font-black text-teal-700">فتح توقيع العميل</a>}</div>)}</div> : null}</div></div></section></div> : null}
  </main>
  {zoomedPhoto ? <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/80 p-4" role="presentation" onClick={() => setZoomedPhoto(null)}><div role="dialog" aria-modal="true" aria-label="تكبير صورة أمر العمل" className="relative max-h-[92vh] w-full max-w-5xl rounded-2xl bg-slate-950 p-2" onClick={event => event.stopPropagation()}><button type="button" aria-label="إغلاق الصورة المكبرة" onClick={() => setZoomedPhoto(null)} className="absolute left-3 top-3 z-10 rounded-full bg-white/90 p-2 text-slate-900 shadow"><X className="h-5 w-5" /></button><img src={zoomedPhoto || undefined} alt="صورة مكبرة من أمر العمل" className="max-h-[86vh] w-full rounded-xl object-contain" /></div></div> : null}
}
