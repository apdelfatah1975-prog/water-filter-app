# دليل المستخدم والتوثيق الفني — نقطة نقاء

**الإصدار:** النسخة المستقرة على فرع `main`  
**اللغة:** العربية RTL  
**المؤلف:** Manus AI  

> يصف هذا الملف الوظائف الموجودة فعليًا. خدمة WhatsApp Cloud API غير مفعّلة حاليًا، ولا توجد في هذا الملف كلمات مرور أو Access Tokens أو بيانات عملاء.

## 1. ما هو النظام؟

نقطة نقاء نظام داخلي لإدارة تركيب وصيانة فلاتر مياه الشرب. يجمع بيانات العملاء والزيارات وأوامر الفنيين والتذكيرات والمخزون والخزينة والتقارير في واجهة عربية مضغوطة مناسبة للكمبيوتر والهاتف. يعتمد المشروع على React 19 في الواجهة، وNode.js/Express مع tRPC في الخادم، وDrizzle ORM مع MySQL في قاعدة البيانات.

## 2. الدخول والصلاحيات

ابدأ من شاشة الدخول. يُنشأ المدير الأول من رابط **إنشاء أول حساب مدير**، ثم يستخدم البريد وكلمة المرور. تُحفظ كلمات المرور كـ Hash آمن مع salt عشوائي، وتُوقّع الجلسات بواسطة `JWT_SECRET` الموجود في Secrets فقط.

| الدور | الوظائف |
|---|---|
| المدير `admin` | لوحة التحكم، العملاء، الزيارات، التذكيرات، المخزون، الخزينة، التقارير، أوامر العمل، الفنيون والإعدادات. |
| الفني `user` | واجهة الفني وأوامر العمل والعمليات المسموح بها، دون صفحات الإدارة المحمية. |

استخدم تسجيل الخروج بعد انتهاء العمل. عطّل حساب الفني فور انتهاء صلاحيته، ولا تشارك حساب المدير. تطبق الواجهة والخادم حماية للمسارات وإسناد البيانات للمالك، لكن يجب اختبار حساب مدير وحساب فني حقيقيين على بيئة التشغيل قبل الاستخدام الميداني.

## 3. لوحة التحكم والعمل اليومي

تعرض الصفحة الرئيسية عملاء اليوم، الزيارات القادمة، المستحقين للمتابعة، وحالة المخزن. تظهر شارة حمراء عند وصول صنف إلى الحد الأدنى أو أقل، ويمكن الضغط عليها لفتح المخزون.

### إضافة عميل وتسجيل زيارة

من **العملاء** أضف الاسم والهاتف والعنوان والموقع GPS والملاحظات. يتيح ملف العميل مراجعة كل الزيارات السابقة، الاتصال، وفتح الموقع على الخريطة.

اختر نوع الزيارة: تركيب فلتر، صيانة، تغيير شمعات، متابعة، أو أخرى. سجّل التاريخ والفني والنتيجة والقراءات والمواد المستخدمة. بعد التركيب أو الصيانة حدد مدة المتابعة أو تاريخها؛ عند ترك الافتراضي ينشئ النظام موعدًا بعد **120 يومًا**، ويظهر الموعد في **التذكيرات**.

## 4. التذكيرات وأوامر الفني

من **التذكيرات** راجع المتأخر واليوم والقادم. قبل الاتصال راجع آخر زيارة، ثم سجّل الزيارة الجديدة من ملف العميل لتجنب تكرار السجل.

واجهة الفني متاحة عبر `/technician-app` أو `/technician-preview` للحساب المصرح. عند إغلاق أمر العمل تُخصم المواد المستخدمة من المخزون، ويُحفظ سجل الحركة باسم الفني والعميل عند توفرهما. يظهر تنبيه فوري إذا وصل الرصيد للحد الأدنى.

## 5. المخزون

تتيح صفحة **المخزون** متابعة الأصناف والرصيد الحالي والوارد والمنصرف والحد الأدنى. كل حركة صرف ينبغي أن تسجل التاريخ والفني المستلم والعميل المرتبط والملاحظة.

| الحقل | المعنى |
|---|---|
| الرصيد الحالي | الكمية بعد احتساب الوارد والمنصرف. |
| الوارد | الكمية الداخلة إلى المخزن. |
| المنصرف | الكمية المستخدمة أو المسلّمة. |
| الحد الأدنى | العتبة التي تفعّل التنبيه الأحمر. |
| سجل الحركة | الصنف والكمية والفني والعميل والتاريخ والملاحظة. |

لا تعدّل الرصيد خارج التطبيق؛ استخدم حركة وارد أو صرف حتى يبقى الجرد وسجل العمليات متطابقين.

## 6. الخزينة والتقارير

من **الخزينة** سجل التحصيلات والمدفوعات مع العميل والزيارة والمبلغ والملاحظة. استخدم **التقارير** لمراجعة الحركات خلال فترة محددة. لا تحذف حركة مالية مكتملة دون إجراء إداري موثق وتسوية واضحة.

## 7. الإعدادات والفنيون

من **الإعدادات** اضبط كثافة عرض العملاء على الهاتف. ومن إدارة الفنيين أنشئ حسابًا وامنحه أقل صلاحية لازمة، ثم عطّله عند انتهاء صلاحيته. لا تستخدم حساب المدير على جهاز الفني.

## 8. ربط WhatsApp Cloud API

**الوضع الحالي:** لا يوجد اتصال نشط بـ Meta WhatsApp Cloud API في النسخة الحالية. تظهر إمكانات التواصل مع أرقام العملاء وروابطها، لكن لا يجوز وضع Phone Number ID أو Access Token في الواجهة أو GitHub. التفعيل الفعلي يحتاج خدمة خادمية للإرسال وWebhook للتحقق من الأحداث وتوقيع الطلبات وتخزين حالة الرسائل.

### متطلبات Meta

تحتاج إلى حساب Meta ومطوّر وتطبيقًا يدعم WhatsApp، وحساب WhatsApp Business، ورقم أعمال يملك القدرة على استقبال SMS أو مكالمة التحقق. توضح Meta أن البدء يتم بإنشاء تطبيق، وربط حساب WhatsApp Business من API Setup، ثم استخدام رمز مؤقت للاختبار [1]. يجب تسجيل الرقم والتحقق من ملكيته قبل الإرسال والاستقبال عبر Cloud API [2].

### خطوات الإعداد

1. افتح [Meta App Dashboard](https://developers.facebook.com/apps)، وأنشئ تطبيقًا باستخدام **Connect with customers through WhatsApp** واربطه بمحفظة الأعمال.
2. افتح **WhatsApp → API Setup**، واربط حساب WhatsApp Business أو أنشئ واحدًا. احتفظ بـ Business Account ID في مدير الأسرار.
3. اختر رقم الاختبار أو أضف رقم الأعمال، ولّد رمزًا مؤقتًا، وأرسل رسالة اختبار. احتفظ بـ Phone Number ID في Secrets فقط.
4. من إعدادات الأعمال أنشئ System User، وامنحه أصول التطبيق وحساب واتساب، ثم أنشئ رمزًا دائمًا بالصلاحيات اللازمة مثل `business_management` و`whatsapp_business_messaging` و`whatsapp_business_management` [1].
5. تحقق من رقم الهاتف وسجّله للاستخدام مع Cloud API. الرقم المستخدم حاليًا مع WhatsApp Messenger قد يحتاج إجراءات Meta قبل التسجيل [2].
6. أنشئ Webhook عامًّا عبر HTTPS يدعم تحقق Meta، ويتحقق من توقيع الطلبات، ويمنع تكرار الأحداث. لا تسجل Access Token أو نصوص العملاء في السجلات.
7. اختبر التسليم وحالة القراءة. خارج نافذة خدمة العميل استخدم قوالب رسائل معتمدة من Meta بدل النص الحر [1].

المتغيرات المقترحة للخادم هي `WHATSAPP_PHONE_NUMBER_ID` و`WHATSAPP_BUSINESS_ACCOUNT_ID` و`WHATSAPP_ACCESS_TOKEN` و`WHATSAPP_VERIFY_TOKEN` و`WHATSAPP_APP_SECRET`. لا تُعرّفها في `client/src` أو ملف Markdown أو GitHub. إذا انكشف رمز، ألغِه من Meta وأنشئ بديلًا.

## 9. التشغيل المحلي للمطورين

يتطلب المشروع Node.js وpnpm وMySQL. انسخ `standalone.env.template` إلى ملف بيئة محلي غير متعقب، واضبط `DATABASE_URL` و`JWT_SECRET` بطول لا يقل عن 32 حرفًا. لا ترفع ملف البيئة.

```bash
pnpm install --frozen-lockfile
pnpm run db:migrate
pnpm run dev
```

| الأمر | الغرض |
|---|---|
| `pnpm run dev` | تشغيل التطوير. |
| `pnpm run test` | تشغيل Vitest. |
| `pnpm run check` | فحص TypeScript. |
| `pnpm run build` | بناء الواجهة والخادم. |
| `pnpm run db:migrate` | تطبيق ترحيلات Drizzle. |

يستمع الخادم إلى `PORT` وعلى `0.0.0.0`. نقطة الصحة هي `GET /health` وتعيد JSON مستقلًا.

## 10. النشر على Render

يحتوي المشروع على `render.yaml` لخدمة Node مع أوامر التثبيت والترحيل والبناء والتشغيل، ويحدد `/health` كمسار فحص. تربط Render مستودع GitHub بفرع `main`، وتضع الأسرار من لوحة الخدمة [3].

تحقق من وجود `DATABASE_URL` في Secrets، وأن `JWT_SECRET` مولّد وقوي، ثم راقب سجل البناء وافتح `/health`. تعتبر Render فحص HTTP سليمًا عند استجابة 2xx أو 3xx خلال المهلة [4]. تستخدم الخدمة المنفذ الموجود في `PORT` وعلى المضيف `0.0.0.0` [3].

اجعل قاعدة البيانات خاصة، فعّل SSL حسب مزود MySQL، وخطط لنسخ احتياطي مستقل واختبار استعادة. لا تحفظ الأسرار أو النسخ الاحتياطية في GitHub.

## 11. قائمة فحص قبل التشغيل

| البند | المطلوب |
|---|---|
| الأسرار | Secrets فقط، دون Tokens أو كلمات مرور في المستودع. |
| الدخول | اختبار حساب مدير وفني منفصلين وتسجيل الخروج. |
| الصلاحيات | الفني لا يصل إلى الإدارة أو بيانات خارج نطاقه. |
| العملاء والمتابعة | إضافة عميل وزيارة وموعد 120 يومًا وفتح الملف. |
| المخزون | الخصم يسجل الفني والعميل والتنبيه يعمل. |
| الصحة | `/health` يعيد HTTP 200. |
| واتساب | لا يُفعّل قبل إعداد Meta وWebhook والقوالب. |
| الحماية التشغيلية | إضافة Rate Limiting وAudit Log قبل التشغيل الميداني الكامل. |

## المراجع

[1]: https://developers.facebook.com/documentation/business-messaging/whatsapp/get-started "Meta — WhatsApp Cloud API Get Started"

[2]: https://developers.facebook.com/documentation/business-messaging/whatsapp/business-phone-numbers/phone-numbers "Meta — Business phone numbers"

[3]: https://render.com/docs/web-services "Render — Web Services"

[4]: https://render.com/docs/health-checks "Render — Health Checks"
